14. Write a C program to check whether a year is a leap year.

#include <stdio.h>

int main() {
    int year;

    printf("Enter a year: ");
    scanf("%d", &year);

    if((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
        printf("%d is a Leap Year.\n", year);
    } else {
        printf("%d is not a Leap Year.\n", year);
    }

    return 0;
}
output:
Enter a year: 2000
2000 is a Leap Year

Enter a year: 1900
1900 is not a Leap Year
